use FMP API, to extract US Stock data, the scanner is detacting KEY AREA (zones(daily weekly,previous day high/low) and its current trading volume, it will output/refresh the result send to discord 
channel, and also at the local UI(python tkinder)


make up your own .env file to run(fmp token, other variables .. )

FMP_API_KEY
DISCORD_WEBHOOK_URL
DAILY_LIMIT
WEEKLY_LIMIT
TOP_PICK
DISCORD_WEBHOOK_URL_DASHBOARD

CHECK_INTERVAL_SECONDS=5

BREAK_MAX_PCT
